const enum Types {
    ADD = 'add',
    MOVE = 'move',
    RUN = 'run',
    RESIZE = 'resize'
}

const enum Sides {
    TL = 'top-left',
    TR = 'top-right',
    BL = 'bottom-left',
    BR = 'bottom-right'
}